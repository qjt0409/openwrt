# SPDX-License-Identifier: MIT
# CI 项目：LEDE x86_64 云编译（J1900 / J1800）
name: OWRT-ALL

# CI 计划
on:
  # 自动编译：每两天北京时间03:12（对应UTC 19:12）
  schedule:
    - cron: 12 19 */2 * *
  # 手动编译
  workflow_dispatch:
    inputs:
      PACKAGE:
        description: '手动调整插件包，多个请用\n符号隔开（例如 CONFIG_PACKAGE_luci-app-openclash=y）。'
        required: false
        type: string
      TEST:
        description: '仅输出配置文件，不编译固件。'
        default: false
        required: false
        type: boolean

# CI 权限
permissions: write-all

# CI 任务
jobs:
  config:
    name: ${{matrix.CONFIG}}
    strategy:
      fail-fast: false
      #max-parallel: 3
      matrix:
        CONFIG: [X86]
        SOURCE: [coolsnowwolf/lede]
        BRANCH: [master]

    # 调用公用编译核心 WRT-CORE.yml
    uses: ./.github/workflows/WRT-CORE.yml
    with:
      # 编译配置
      WRT_CONFIG: ${{matrix.CONFIG}}
      # 默认主题（Argon）
      WRT_THEME: argon
      # 默认主机名
      WRT_NAME: LEDE
      # 默认 WIFI 名称（x86 无无线，仅占位）
      WRT_SSID: LEDE
      # 默认 WIFI 密码（x86 无无线，仅占位）
      WRT_WORD: 12345678
      # 默认登录地址
      WRT_IP: 10.0.1.1
      # 登录密码说明（仅作提示；默认无密码，首次登录自行设置）
      WRT_PW: 首次登录自行设置
      # 源码链接
      WRT_REPO: https://github.com/${{matrix.SOURCE}}.git
      # 源码分支
      WRT_BRANCH: ${{matrix.BRANCH}}
      # 源码名称
      WRT_SOURCE: ${{matrix.SOURCE}}
      # 插件调整
      WRT_PACKAGE: ${{inputs.PACKAGE}}
      # 仅输出配置文件
      WRT_TEST: ${{inputs.TEST || false}}
