#====================================================
# 通用参数
#====================================================
CONFIG_CCACHE=y
CONFIG_DEVEL=y
CONFIG_TARGET_ROOTFS_INITRAMFS=n

#====================================================
# 科学上网
# 来源：Openwrt-Passwall 官方仓库（Scripts/Packages.sh 拉取，并已处理与 feeds 的重名冲突）
#====================================================
CONFIG_PACKAGE_luci-app-passwall=y
CONFIG_PACKAGE_luci-app-passwall2=y

#====================================================
# 存储 / 文件 / 下载
#====================================================
# iStore 商店（linkease/istore）
CONFIG_PACKAGE_luci-app-store=y
# 易有云文件管理器（linkease/luci-app-linkease）
CONFIG_PACKAGE_luci-app-linkease=y
# alist 文件列表（sbwml/luci-app-alist 前端 + packages 源 net/alist 后端）
CONFIG_PACKAGE_luci-app-alist=y
# openlist（luci feed 内置，前端 + packages 源 net/openlist 后端）
CONFIG_PACKAGE_luci-app-openlist=y
# lucky（luci feed 内置 + packages 源 net/lucky 后端）
CONFIG_PACKAGE_luci-app-lucky=y
# 磁盘管理（luci feed 内置）
CONFIG_PACKAGE_luci-app-diskman=y

#====================================================
# 消息推送
#====================================================
# 全能推送（zzsj0928/luci-app-pushbot）
CONFIG_PACKAGE_luci-app-pushbot=y
# 微信推送（tty228/luci-app-serverchan，包名为 luci-app-wechatpush）
CONFIG_PACKAGE_luci-app-wechatpush=y

#====================================================
# DDNS
#====================================================
# ddns-go（luci feed 内置前端 + packages 源 net/ddns-go 后端）
CONFIG_PACKAGE_luci-app-ddns-go=y

#====================================================
# 行为管理 / 限速 / 加速
#====================================================
# IP 限速（sirpdboy/luci-app-eqosplus；luci feed 内置的 luci-app-eqos 依赖高通 NSS 模块，x86 不可用，已禁用）
CONFIG_PACKAGE_luci-app-eqosplus=y
CONFIG_PACKAGE_luci-app-eqos=n
# TurboACC 网络加速（luci feed 内置）：Flow Offloading + BBR（内核 6.18 自带最新 BBR，首次启动自动启用）
CONFIG_PACKAGE_luci-app-turboacc=y
CONFIG_PACKAGE_TURBOACC_INCLUDE_FLOW_OFFLOADING=y
CONFIG_PACKAGE_TURBOACC_INCLUDE_BBR_CCA=y
CONFIG_PACKAGE_TURBOACC_INCLUDE_FAST_CLASSIFIER=n
CONFIG_PACKAGE_TURBOACC_INCLUDE_SHORTCUT_FE_CM=n
CONFIG_PACKAGE_TURBOACC_INCLUDE_NO_FASTPATH=n
# 全锥形 NAT（TurboACC 会自动检测 xt_FULLCONENAT.ko 并开启 fullcone）
CONFIG_PACKAGE_kmod-ipt-fullconenat=y
CONFIG_PACKAGE_iptables-mod-fullconenat=y

#====================================================
# 系统基础功能
#====================================================
# uhttpd Web 服务管理（luci feed 内置，官方 luci-app-uhttpd）
CONFIG_PACKAGE_luci-app-uhttpd=y
# 负载均衡（luci feed 内置 + packages 源 net/mwan3）
CONFIG_PACKAGE_luci-app-mwan3=y
# TTYD 终端（luci feed 内置 + packages 源 utils/ttyd）
CONFIG_PACKAGE_luci-app-ttyd=y
# 自定义命令（luci feed 内置）
CONFIG_PACKAGE_luci-app-commands=y
# 网络共享 Samba4（luci feed 内置 + packages 源 net/samba4）
CONFIG_PACKAGE_luci-app-samba4=y
# 流量统计（luci feed 内置 + packages 源 collectd/rrdtool1）
CONFIG_PACKAGE_luci-app-statistics=y
# Docker（中文前端 luci-app-docker [lisaac] + packages 源 docker/dockerd/docker-compose）
CONFIG_PACKAGE_luci-app-docker=y
CONFIG_PACKAGE_docker=y
CONFIG_PACKAGE_dockerd=y
CONFIG_PACKAGE_docker-compose=y
# UPnP（luci feed 内置）
CONFIG_PACKAGE_luci-app-upnp=y
# 定时重启（luci feed 内置）
CONFIG_PACKAGE_luci-app-autoreboot=y

#====================================================
# Lua 兼容层（passwall/serverchan/linkease/eqosplus 等 Lua 插件需要）
#====================================================
CONFIG_PACKAGE_luci-compat=y
CONFIG_PACKAGE_luci-lua-runtime=y
CONFIG_PACKAGE_luci-lib-jsonc=y
# 简体中文语言包（LuCI 界面默认中文）
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y

#====================================================
# 内核模块
#====================================================
# SATA/NVMe 磁盘
CONFIG_PACKAGE_kmod-ata-ahci=y
CONFIG_PACKAGE_kmod-ata-core=y
CONFIG_PACKAGE_kmod-nvme=y
CONFIG_PACKAGE_kmod-fs-btrfs=y
CONFIG_PACKAGE_kmod-fuse=y
# USB（含 USB 网卡，软路由常用）
CONFIG_PACKAGE_kmod-usb3=y
CONFIG_PACKAGE_kmod-usb-core=y
CONFIG_PACKAGE_kmod-usb-ehci=y
CONFIG_PACKAGE_kmod-usb-ohci=y
CONFIG_PACKAGE_kmod-usb-uhci=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-usb-storage-extras=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y
CONFIG_PACKAGE_kmod-usb-net=y
CONFIG_PACKAGE_kmod-usb-net-asix-ax88179=y
CONFIG_PACKAGE_kmod-usb-net-rtl8152=y
CONFIG_PACKAGE_kmod-usb-net-rtl8150=y
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y
# 虚拟网卡 / 隧道 / 组网
CONFIG_PACKAGE_kmod-tun=y
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_kmod-wireguard=y
CONFIG_PACKAGE_kmod-bonding=y
CONFIG_PACKAGE_kmod-dummy=y
# 网络加速（Flow Offload，iptables/nftables 双栈）
CONFIG_PACKAGE_kmod-ipt-offload=y
CONFIG_PACKAGE_kmod-nft-offload=y
# 诊断
CONFIG_PACKAGE_kmod-inet-diag=y
CONFIG_PACKAGE_kmod-netlink-diag=y

#====================================================
# 常用组件
#====================================================
CONFIG_PACKAGE_blkid=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_fdisk=y
CONFIG_PACKAGE_sfdisk=y
CONFIG_PACKAGE_partx-utils=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_ca-bundle=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_dmesg=y
CONFIG_PACKAGE_xz-utils=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_openssl-util=y
